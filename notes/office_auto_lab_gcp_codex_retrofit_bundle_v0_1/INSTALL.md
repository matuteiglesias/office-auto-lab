# Installation note

Copy the `docs/` directory from this bundle into the repository root.

Then tell Codex:

> Read `docs/retrofit/gcp_project_health/CODEX_START_HERE.md` and execute only `PR-G0`. Update its closure note and propose the carry-state transition. Stop when the PR is ready for review.
