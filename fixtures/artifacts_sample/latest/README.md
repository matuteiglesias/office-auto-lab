# Synthetic Office artifacts fixture

This directory contains fake, non-sensitive sample artifacts for Office Window frontend development. It is intentionally separate from the real `artifacts/` output tree and must not be treated as production data.

All project IDs, workdirs, titles, logs, counts, and brief content are synthetic.
