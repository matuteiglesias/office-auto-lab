Machine readable spec for compiler v0.

These files are the ground truth for enums, operator registry subset, archetype templates, prepared block schema, and classification rules.

Human rationale and decisions live in: notes/compiler_v0_spec.md
